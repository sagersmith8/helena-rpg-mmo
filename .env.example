# Backend API base URL (PostgREST or Supabase REST). No trailing slash.
EXPO_PUBLIC_API_URL=http://localhost:3000

# Base URL for static assets (icons, images). Include trailing slash.
# With docker-compose this is nginx on 3001; with Supabase use Storage URL.
EXPO_PUBLIC_IMAGE_HOST=http://localhost:3001/
